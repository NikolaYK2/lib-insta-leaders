/**
 * Maximum screen width (in pixels) at which
 * the application should switch to mobile-specific styles or behaviors.
 *
 * @constant {number}
 * @default 360
 */
export const MOBILE_BREAKPOINT = 360

/**
 * Maximum screen width (in pixels) at which
 * the application should switch to tablet-specific styles or behaviors.
 *
 * @constant {number}
 * @default 768
 */
export const TABLET_BREAKPOINT = 768

/**
 * Maximum screen width (in pixels) at which
 * the application should switch to small desktop-specific styles or behaviors.
 *
 * @constant {number}
 * @default 1024
 */
export const SMALL_DESKTOP_BREAKPOINT = 1024

/**
 * Minimum screen width (in pixels) at which
 * the application should switch to large desktop-specific styles or behaviors.
 *
 * @constant {number}
 * @default 1440
 */
export const LARGE_DESKTOP_BREAKPOINT = 1440
